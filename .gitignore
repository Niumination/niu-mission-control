# Python (legacy — masih dipakai untuk transisi; akan dihapus setelah M2 stabil)
__pycache__/
*.pyc
venv/
.venv/

# Environment (berisi secrets — JANGAN commit)
.env
.env.local
.env.*.local
.apex-ui/.env
.apex-ui/.env.local
apex-ui/.env
apex-ui/.env.local
apex-ui/.env.*.local

# SQLite (runtime state — jangan commit)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
data/swarm_state.db*
data/backups/

# Runtime data
data/*.json
data/contents/

# OS
.DS_Store
Thumbs.db

# Temp & logs
/tmp/hermes_*
*.log
cookies.txt
npm-debug.log*

# Legacy file backups (jangan commit)
dashboard/index.html.bak*
dashboard_backup_refactor_*/

# IDE
.vscode/settings.json
.idea/

# Frontend legacy assets (dihapus 24 Sep 2026)
frontend/static/fontawesome/fontawesome-free-6.5.1-web/

# Next.js build output
.next/
out/

# Node
node_modules/
apex-ui/node_modules/

# Catatan 26 Sep 2026: `docs/deploy-launchagent.sh` TIDAK di-ignore.
# File ini sudah tracked sejak v3 dan hanya berisi path lokal + perintah
# launchctl (nol kredensial — dicek 26 Sep). Meng-ignore-nya tidak mencegah
# apa pun karena file sudah terlacak, tapi membuat
# scripts/secret-scan-staged.py menolak commit dan membingungkan.
# Kalau nanti memang perlu dirsphinx, pindahkan ke git-ignored `deploy/`
# lokal — jangan andalkan .gitignore untuk file yang sudah tracked.

# Standalone Next.js build
apex-ui/.next/standalone/

# TypeScript build info
apex-ui/tsconfig.tsbuildinfo
