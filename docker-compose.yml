services:
  mission-control:
    build: .
    ports:
      - "5200:5200"
    volumes:
      - ./data:/app/backend/data
      - ./.env:/app/backend/.env
    environment:
      - MC_DEBUG=0
    restart: unless-stopped
