'use client'

/**
 * ToastPlayground — file ini tidak digunakan di production.
 * Cuma berfungsi sebagai referensi API untuk dev:
 *   import { toast } from '@/components/ui/Toast'
 *   toast.success('Task created')
 *   toast.error('Failed to dispatch')
 *   toast.info('Reconnected')
 *   toast.warning('Approval required')
 */

export {}
